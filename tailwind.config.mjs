/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  // In Tailwind v4, most theme configuration should be in CSS using @theme
  // Keep this minimal or remove it entirely
}